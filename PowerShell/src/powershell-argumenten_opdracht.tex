
\begin{enumerate}
\item We gaan voor deze opdrachten werken met de uitkomsten van het PowerShell commando:
\begin{lstlisting}[language=bash]
Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion"
\end{lstlisting}
\item Schrijf een PowerShell script dat via parameters de verschillende stukjes informatie produceert. We willen CurrentBuild, DisplayVersion, ProductName als losse data aan het script kunnen vragen.
\end{enumerate}

