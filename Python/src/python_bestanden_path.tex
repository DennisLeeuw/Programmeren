Om te zien of een pad of en bestand bestaat op een systeem kunnen we gebruik maken van de \texttt{os}-module of van de \texttt{pathlib}-module. We zullen van beide een voorbeeld geven. Het heeft de voorkeur om de moderne versie met \texttt{pathlib} te gebruiken.

De versie met de \texttt{os}-module:
\lstinputlisting[language=python]{code/path_os.py}

De versie met de {pathlib}-module:
\lstinputlisting[language=python]{code/path_pathlib.py}

We zullen in de rest van dit document alleen nog \texttt{pathlib} gebruiken.

