Om een bestand weg te gooien (delete) hebben we de \texttt{os}-module nodig waaruit we de \texttt{remove} functie gebruiken:
\begin{lstlisting}[language=python]
import os
os.remove("demofile.txt")
\end{lstlisting}

