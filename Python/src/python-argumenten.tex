We hebben bij functies gezien dat deze argumenten kunnen accepteren, dat kan ook bij scripts. Om de argumenten uit te kunnen lezen hebben we een module nodig die \texttt{sys} heet. In een list met de naam \texttt{argv} zit de bestandsnaam (argv[0]) en de argumenten.

Een programma om alle argumenten weer te geven:
\lstinputlisting[language=Python]{code/argv.py}

Het opstarten kan met:
\begin{lstlisting}[language=bash]
python argv.py a b c
\end{lstlisting}
