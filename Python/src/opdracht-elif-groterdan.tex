Met de tekens > en < kan je kijken of een getal groter dan of kleiner dan een bepaalde waarde is. Maak gebruik van het onderstaande script en vul dit aan. Als het getal groter is dan 6 print op het scherm dat het getal groter is dan 6, en als het getal kleiner is dan 5 print dat op het scherm.
\begin{lstlisting}[language=python]
a = input("Geef een getal tussen 0 en 10"}

if a ...
\end{lstlisting}

